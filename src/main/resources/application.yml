server:
  port: 4646
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/project_db
    username: root 
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
     #nothing